# 后处理指南：从"第一遍结果"到可用的 CDR

`cdr_vectorize` 的一次调用只是**第一遍**。它会尽量自动做好，同时把拿不准的地方**报告出来**。
效果好不好，很大程度取决于调用它的 AI（或你自己）有没有处理这些报告。

**直接调一次就结束 = 跳过了一半的工作。** 大多数"效果不好"都出在这里。

整个流程分两部分：

1. **AI 核对与重建**（第 1–4 节）：修正文字、补漏字，调用 `cdr_vectorize_build` 重建。不用重新超分和 OCR，一般一两分钟。
2. **在 CorelDRAW 里手工收尾**（第 5 节）：换字体、微调位置、合并图层等，自动流程不擅长的细节。

---

## 1. 先看返回值里的这几个字段

| 字段 | 含义 | 为空/为 false 时 |
|---|---|---|
| `workflow` | **第一个字段**。`step` 取值 `review` / `running` / `blocked` / `converged` / `delivered` / `failed`；`must_do_next` 是接下来必须做的事 | 不会为空，先读它 |
| `to_fill` | **故意留空**的位置，每项含 `text`、`box`、`why`。CDR 里对应 `TO FILL` 图层上的品红框；`to_fill_marks` 是框的数量 | 没有留空 |
| `labels_to_check` | 不确定的文字，每项含 `id`（L#）、`text`、`box`、`placed`、`why`、`other_readings` | 文字都有把握 |
| `unlabeled_text` | 疑似文字、但没被识别出来的区域，每项含 `id`（M#）、`box` | 没发现漏字 |
| `review_sheet` | 一张核对图：每个 L#/M# 的原图截图（红框为识别框）和当前读数 | 无需核对 |
| `graphics_issues` | **加字之前**、在纯图形结果上发现的问题（含 `wipe_spill`），见第 3 节 | 描摹没问题 |
| `graphics_check` | 图形阶段的指标与差异图 `graphics_check.png` | 同上 |
| `text_issues` | 文字对象本身放错了（重叠、字号、缺字） | 文字都放对了 |
| `label_placement` | 每个标签预先算好的字号 / 角度 / 位置 / 颜色，见第 2.5 节 | — |
| `wipe_spill` | 标签框把线稿当文字擦掉的像素数与区域（**只管墨层那次擦除**，彩色文字那次走 inpaint，它看不到） | 没有挖坏图形 |
| `issues` | 上面图形类与文字类问题的合并列表（兼容旧调用方），见第 3 节 | 都没发现问题 |
| `self_check.diff_png` | 差异图：**红 = 原图有、结果缺失**，**蓝 = 结果多出来**，橙 = 颜色不对 | — |
| `labels` | 本次实际使用的文字列表，**修正时就在这份上改** | — |
| `work_dir` | 工作目录，重建时要传回去 | — |

> **图不用自己去打开。** `review_sheet`、`self_check.diff_png` 等会以 MCP 图片内容**直接返回**，
> 就在 AI 的上下文里。看到图之后再决定怎么改，不要凭字段猜。

所有 `box` 都是 `[x0, y0, x1, y1]`，单位是 **`source_png` 的像素**。大图会先缩小，缩放比例见 `source_scale`；手工量坐标时请在 `source_png` 上量，不要在原始输入图上量。`label_placement` 里的 `box` / `tight` / `place.center_px` 同样是**源图像素**。

---

## 2. 修正文字（最常见，也最值得做）

打开 `review_sheet`，逐项处理：

### L#：不确定的文字
- **读错了**：在 `labels` 里找到 `box` 相同的那一项，把 `text` 改成正确的字。`other_readings` 里常常就有正确答案。
- **读对了**：什么都不用做。
- **不是文字**（其实是符号、图例块、剖面线）：从 `labels` 里删掉这一项，它就会按图形描摹。
- **`placed: false`**（因为太不确定没有放上去）：如果确实是文字，把 `{"text": "...", "box": [...]}` 加进 `labels`。
- **框没框全**（红框只框住一部分字）：把 `box` 扩大到整个标签。
- **一个框里有好几个标签**（例如 `集流环测井仪器车测井仪器板`）：用竖线分开写，`"集流环|测井仪器车|测井仪器板"`，重建时会在字间空隙处自动切开。

### M#：漏识别的文字
看截图，是文字就在 `labels` 里加一项：

```json
{"text": "藏", "box": [193, 357, 208, 372]}
```

不是文字（符号、线头）就忽略。

### 斜排、竖排文字
可以在该项里加 `angle`（单位：度，图像坐标下顺时针为正）。竖排的坐标轴标题通常是 `-90`：

```json
{"text": "中介中心性", "box": [6, 362, 49, 537], "angle": -90}
```

---

## 3. 处理图形问题

**先分清是图形问题还是文字问题**，两者的修法完全不同，混在一起改只会越改越乱：

| 报告 | 什么时候测的 | 怎么修 |
|---|---|---|
| `graphics_issues` | **加字之前**，页面上只有描摹出来的图形 | `mode` / `colors` / 换更清晰的原图；改标签没用 |
| `text_issues` | 加字之后 | 改 `labels` 里的 `text` / `box` |
| `wipe_spill` | 擦字阶段 | **只有改框能救**（缩小或删掉那个标签）；改文字没用 |
| `label_placement` | 描摹之前就算好 | 核对字号/角度/位置；不对就改对应标签的 `box` 或 `angle` |

`graphics_issues` 是在没有文字的页面上测的，所以它报的每一处缺失都**必然**是描摹问题——
"线丢了"和"文字把线盖住了"在最终结果上长得一模一样，只有分开测才能判断该动哪一边。

每条 issue 都带 `detail`（问题描述）、`regions`（区域坐标）和 `hint`（修复建议）。对照 `self_check.diff_png` 看最直观。

| type | stage | 表现 | 通常的原因 | 怎么处理 |
|---|---|---|---|---|
| `text_mismatch` | `text` | 放上去的文字回读和原图不一致：叠字、字号明显不对、缺字 | 识别框不准，或和相邻图形挤在一起 | 修正该标签的 `box`/`text` 后重建；字号问题也可以留到 CorelDRAW 里手调 |
| `lost_elements` | `graphics` | 小点、小符号、小色块整块丢了或颜色不对 | 颜色太少被当成噪点，或被当成文字擦掉了 | 若被误当成文字：从 `labels` 删掉覆盖它的标签；颜色没分出来：`colors` 调大（如 16）重建 |
| `graphics_missing` | `graphics` | 大面积内容没画出来 | 模式不对，或某个图层描摹失败 | 换 `mode` 重建：彩图丢颜色用 `"color"`，细线丢了用 `"lineart"` |
| `graphics_extra` | `graphics` | 多出原图没有的东西 | 通常是没擦干净的文字残留（漏识别的字） | 按 M# 补上漏掉的文字，重建后残留会被擦掉 |
| `colour_mismatch` | `graphics` | 颜色对不上 | 颜色分层数不够 | `mode="color"`，`colors` 调大重建 |
| `trace_failed` | `graphics` | 某个图层完全没描摹出来 | PowerTRACE 失败（多见于超大图） | 先直接重建一次；仍失败时把原图缩小后重新跑 |
| `wipe_spill` | `labels` | 标签框外的线稿被当成文字擦掉了，结果里不可能出现 | 标签框太大，或这个框本来是给线稿的 | **只有改框能救**：缩小或删掉那个标签后重建；改它的文字没有任何作用 |

---

## 4. 重建

**用 `label_patch` 只传改动，不要重抄整份 `labels`**：

```text
cdr_vectorize_build(work_dir, label_patch={"set": {"L3": "藏"}, "drop": ["L7"], "add": [...]})
```

| 键 | 作用 |
|---|---|
| `set` | `{L#: 新文字}`。空串表示"不是文字"，该框交回图形描摹 |
| `drop` | `[L#, ...]`，删掉这些标签 |
| `set_box` | `{L#: [x0, y0, x1, y1]}`，只在这个框确实错时才动 |
| `add` | `[{"text","box","angle"?}]`，补上漏识别的文字（M#） |

为什么不要重抄：改两个字却要重发一百项，是 O(n) 的成本做 O(1) 的事——而且重发本身就会让条目被截断、
顺序错乱或打错字，**把本来对的列表抄坏**。补丁动不到它没提到的东西。

- 超分和 OCR 结果都会复用，只重新分层、描摹、放文字。
- 需要时再加参数：`mode="color"` / `"lineart"`；`colors=16`（彩色层数）；`font="黑体"`（中文字体，可选 `新宋体` / `仿宋` / `黑体` / `微软雅黑`）。
- 重建后会返回新的 `issues`，**再检查一遍**。一般一到两轮就收敛了。
- `skip_uncertain=True`：**只放有把握的文字**，不确定的读数一律留空，改走 `to_fill` + CDR 里的品红框，
  由人工填。**一个读错的标签要满图去找它，一个空位置一眼就看得见**——不确定项多的时候，这比逐个猜省事得多。
- `labels=[]` 表示整张图不放文字，全部当图形描摹。适合只有少量文字、又都很难识别的图。
- 传了 `label_patch` 或 `labels` 就一定会真的重建；不传才会复用上一次的结果。

---

## 4.5 交付确认（`cdr_finish`）

收敛后**必须**调用：

```text
cdr_finish(work_dir)
```

它会检查第 1–4 节该做的事做完没有，只要还有没处理的 L#/M#、`graphics_issues` 或 `text_issues`，
就**返回错误并拒绝确认**，同时列出还差什么。这就是"跳过核对就交付不了"——不是靠提醒，是靠检查。

确认通过后返回：

- `result_cdr` / `result_png`：最终文件
- `remaining_issues`：随交付保留的已知问题
- `must_report_to_user`：要求把这些如实写进给用户的答复

确实无法自动修的（窄条状点状填充被误判成文字、原图本身字高只有 6 像素等），走逃生舱：

```text
cdr_finish(work_dir, accept_remaining=True, note="为什么这些修不了")
```

它照常交付，但 `remaining_issues` 仍在返回值里——**留给用户的说明不能省**。

> 如果某一步返回了 `done=false` / `status="running"`，说明时间预算用完了、任务还在后台跑，
> 这不是失败：调 `cdr_job_status(work_dir)` 每隔 30–60 秒取一次，直到 `done=true`。

---

## 5. 在 CorelDRAW 里手工收尾

打开 `result.cdr`。对象的组织方式如下：

| 对象 | 说明 |
|---|---|
| 每种颜色一个**群组** | 色块，已填色，可能带深色描边 |
| 网格线群组 | 矢量折线，可以统一改线宽、颜色 |
| 黑色墨线群组 | 主要线条（描摹结果） |
| 点状填充/短剖面线群组 | 直接生成的矢量圆点和线段 |
| 每个标签一个**美术字对象** | 可以直接双击改字 |
| **`TO FILL (delete after typing)` 图层** | **只在有 `to_fill` 时出现**。里面是品红空心框，标出故意留空、等你打字的位置；每个框对应 `to_fill` 里的一项 |

常见的收尾操作：

- **补 `to_fill` 的留空**：双击 `TO FILL` 图层里的品红框直接打字（框的尺寸就是建议的容纳范围）。
  填完**把整个图层删掉**——它只是标记，不属于成品。`to_fill` 每项都带 `why`，说明它是读数不确定、
  还是斜排标签量不出长度所以定不了字号。
- **统一换字体**：`编辑 → 查找并替换 → 替换文本属性`，或者选中所有文字后改字体。原图是黑体、微软雅黑一类无衬线字体时，把默认的新宋体换掉，效果提升最明显。
- **加粗模拟**：文字的加粗是用同色描边模拟的。换了真正的粗体字体后，可以在"轮廓笔"里把描边去掉。
- **字号/位置微调**：个别标签偏大、偏移，直接拖动或改字号。坐标轴刻度数字最常需要调。
- **丢失的空格**：OCR 结果不含空格，标题里的空格需要手动补。
- **合并同色区域**：选中某个颜色群组 → `对象 → 造形 → 合并`，节点数会少很多。
- **简化节点**：线条抖动时，用形状工具选中节点，点"减少节点"。
- **核对网格和线宽**：网格是按原图测量的灰度和线宽画的，需要时可以统一调整。

---

## 6. 什么样的图效果好

| 因素 | 建议 |
|---|---|
| 分辨率 | 字高至少 **10 像素**；字高只有 6–7 像素时很难识别，建议找更清晰的原图 |
| 格式 | 尽量用 PNG；JPEG 的压缩色块会让浅色、小色块褪色 |
| 裁剪 | 只保留需要的部分，去掉截图边框、界面按钮 |
| 大图 | **超分倍数会自动选择**：源图 ×2 若会超出 6 MP 工作上限，就直接用原分辨率（不超分、也不缩小）。因为放大后被缩回去等于白做，还要倒赔约 60% 的像素、字高减半。实测 2048×1824 的图：S=2 时 OCR 不确定项 31、图形 precision 0.666、丢 23 个元素；S=1 时 13 / 1.0 / 0。可用 `CDR_SR` 手动覆盖 |
| 最小字高 | **字太小是硬墙**：字高低于 ~8 像素时，文字和短线段（虚线、剖面线）在像素层形状上就无法区分，任何判据都会互相误伤。这种图建议先找更清晰的原图 |
| 扫描件 | 可以直接用，纸张底色会自动校正；严重倾斜的先摆正 |
| 照片、手绘、渐变很多的图 | 不在设计范围内，效果不保证 |

---

## 7. 给调用方 AI 的指令

**这一节已经内置在服务端的 MCP `instructions` 里**：支持该字段的客户端（Claude Code / Claude Desktop 等）
连接时会自动拿到，通常不用再手动配置。如果你的客户端不读 `instructions`，把下面这段加进系统提示词或
项目说明即可，效果相同。另外，`review_sheet` 等图会**直接以图片内容返回**，不需要 AI 自己去打开文件——
所以第 2 步 a 里的"打开"其实是指"看上下文里已经有的那张图"。

```text
使用 cdr MCP 把图片转成 CorelDRAW 文件时：
1. 调用 cdr_vectorize(image_path)。先读返回的 workflow 字段，它直接说明这次是已完成、只做了一半、还是还在跑。
   如果它是 done=false / status="running"，那不是失败：任务还在后台跑，隔 30–60 秒调一次
   cdr_job_status(work_dir)，直到 done=true，不要重开一次 cdr_vectorize。
2. 当 workflow.step 是 "review"：
   a. 看上下文里的 review_sheet 图（它已经作为图片返回了，不用去打开文件），逐个核对
      labels_to_check（L#）和 unlabeled_text（M#）；读错的改 text，不是文字的删掉，
      漏掉的文字按 {"text","box"} 补上，一个框里有多个标签的用 "a|b|c" 分开。
   b. 分清 graphics_issues 和 text_issues：前者换 mode/colors 或改框，后者改 labels，互不替代。
      有 wipe_spill 时只有改框能救，改文字没用。
   c. 用 label_patch 改，不要重抄整份 labels：
      cdr_vectorize_build(work_dir, label_patch={"set": {"L3": "藏"}, "drop": ["L7"], "add": [...]},
                          需要时加 mode/colors)。
   d. 不要动 to_fill：那些是**故意留空**的位置（读数不可靠，或斜排标签量不出长度所以定不了字号），
      CDR 里在 "TO FILL" 图层上用品红框标出，to_fill_marks 是框的数量。它们不是失败，重跑也填不上——
      由人在 CorelDRAW 里打字，然后删掉那个图层。在答复里说明这一点，不要为此重试。
   e. 重复 a–c，直到 workflow.step 变成 "converged"（最多 3 轮）。
3. 调用 cdr_finish(work_dir) 交付。它会在还有未处理项时拒绝确认，所以这是必须走的一步。
   它交付后，把返回的 remaining_issues 如实写进给用户的答复，并说明哪些适合在 CorelDRAW 里手工收尾
   （字体、字号、OCR 丢失的空格）。确实无法自动修的项用 cdr_finish(accept_remaining=True, note="原因")。
```
